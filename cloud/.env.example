# 49Agents Cloud Server — Environment Variables
#
# Copy this file to .env and adjust as needed.
# For local single-user use, NO variables are required — defaults work out of the box.

# ---------------------------------------------------------------------------
# Server
# ---------------------------------------------------------------------------
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# ---------------------------------------------------------------------------
# Database (SQLite)
# ---------------------------------------------------------------------------
# DATABASE_PATH=./data/tc.db

# ---------------------------------------------------------------------------
# Authentication (optional — leave blank for local no-auth mode)
#
# When no OAuth provider is configured, the server runs in local mode:
# no login screen, no accounts, a dev user is created automatically.
#
# To enable auth (recommended for multi-user or internet-exposed setups),
# configure at least one OAuth provider below.
# ---------------------------------------------------------------------------

# GitHub OAuth — https://github.com/settings/applications/new
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# GITHUB_CALLBACK_URL=http://localhost:3001/auth/github/callback

# Google OAuth — https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_CALLBACK_URL=http://localhost:3001/auth/google/callback

# ---------------------------------------------------------------------------
# JWT Secrets (change these in production!)
# ---------------------------------------------------------------------------
# JWT_SECRET=change-me-to-a-random-string
# AGENT_JWT_SECRET=change-me-to-a-different-random-string

# ---------------------------------------------------------------------------
# Optional — LemonSqueezy billing (not needed for self-hosting)
# ---------------------------------------------------------------------------
# LEMONSQUEEZY_WEBHOOK_SECRET=

# ---------------------------------------------------------------------------
# Optional — Discord webhook for notifications
# ---------------------------------------------------------------------------
# DISCORD_WEBHOOK_URL=
# FEEDBACK_WEBHOOK_URL=

# ---------------------------------------------------------------------------
# Optional — Admin
# ---------------------------------------------------------------------------
# ADMIN_USER_ID=

# ---------------------------------------------------------------------------
# Optional — Hosting config (only needed for multi-domain setups)
# ---------------------------------------------------------------------------
# CLOUD_HOST=localhost:3001
# APP_HOST=
# LANDING_DIR=
